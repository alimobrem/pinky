apiVersion: v1
kind: Pod
metadata:
  name: pinky-security-context-patch
  namespace: default
  labels:
    app.kubernetes.io/name: pinky
spec:
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
  - name: pinky
    image: PLACEHOLDER
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
        - ALL
